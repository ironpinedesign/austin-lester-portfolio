import type {Metadata} from 'next';
import Link from '../components/SiteLink';
import {getContent} from '../../lib/content';
import {InlineCopy} from '../components/Copy';
export const dynamic='force-dynamic';
export const metadata:Metadata={title:'Contact',description:'Connect with Austin Lester about strategy, creative direction, design, and technology.'};
export default async function Contact(){const {text}=await getContent();const details={contact_email:text("contact.direct.email"),location:text("contact.direct.location"),linkedin:text("contact.direct.linkedin")};return <><section className="wrap page-opening contact-opening"><p className="eyebrow">{text("contact.opening.eyebrow")}</p><h1><InlineCopy text={text("contact.opening.headline")}/></h1><div className="opening-bottom"><p>{text("contact.opening.body")}</p></div></section><section className="wrap contact-channels"><p className="eyebrow">{text("contact.direct.eyebrow")}</p><div>{details.contact_email?<a className="contact-email" href={`mailto:${details.contact_email}`}>{details.contact_email} ↗</a>:<div className="contact-pending"><h2>{text("contact.direct.pending_heading")}</h2><p>{text("contact.direct.pending_body")}</p></div>}{details.location&&<p className="contact-location">{details.location}</p>}{details.linkedin&&<a className="text-link" href={details.linkedin} target="_blank" rel="noopener noreferrer">{text("contact.direct.linkedin_label")}</a>}</div></section><div className="wrap contact-back"><Link className="text-link" href={text("contact.navigation.work.destination")}>{text("contact.navigation.work.label")}</Link></div></>}
